[tool.poetry]
name = "new_flask"
version = "0.1.0"
description = "Learning flask app"
authors = ["Yulee"]

[tool.poetry.dependencies]
python = "^3.10"
Flask = "^3.0.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
